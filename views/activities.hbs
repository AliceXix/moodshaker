<section>
  <h1>Results tailored to your mood...</h1>

  <div>

    {{#if data}}
      {{#each data}}
        <div>
          <h3>{{this.title}}</h3>
          <a href="/activities/{{this.id}}/details">More details</a>
            {{!-- {{#each this.mood}}
            <p>{{this}}</p> 
            {{/each}}        --}}
        </div>
      {{/each}}

    {{else}}
      <p>we do not have a result for you mood and level of energy.. yet!</p>

    {{/if}}

  </div>

  <div>
    <button>
      <a href="activities/create">Create</a>
    </button>

    <button>
      <a href="/mood-shaker">Shake again !</a>
    </button>

  </div>

</section>